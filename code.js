import { Hero } from "./hero";

var input = document.getElementById("input")

toolbox = new Hero();

function warrior (){
    if (input = warrior)
        then (strength +5)
    if (input = Warrior)
        then (strength +5)
}

function rogue () {
    if (input = rogue)
        then (dexterity +5)
    if (input = Rogue)
        then (dexterity +5)
}

function mage (){
    if (input = mage)
        then (intelligence +5)
    if (input = Mage)
        then (intelligence +5)
}
console.log(Hero)